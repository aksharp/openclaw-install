apiVersion: v2
name: openclaw
description: OpenClaw gateway and optional Vault + observability + Ingress (HAProxy + cert-manager) — single config, single Helm command (V10)
type: application
version: 0.1.0
appVersion: "1.0.0"
keywords:
  - openclaw
  - ai
  - gateway
  - signal
  - vault
  - observability
home: https://github.com/openclaw/openclaw
sources:
  - https://github.com/openclaw/openclaw
maintainers:
  - name: OpenClaw
    url: https://docs.openclaw.ai
dependencies:
  - name: kubernetes-ingress
    version: "1.46.1"
    repository: "https://haproxytech.github.io/helm-charts"
    condition: haproxy.enabled
  - name: cert-manager
    version: "v1.16.2"
    repository: "https://charts.jetstack.io"
    condition: certManager.enabled
